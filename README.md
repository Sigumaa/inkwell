# RPG Engine

Claude Code上で動作するテキストRPG。Saga & Seekerライクな体験をサブスク範囲内で実現。

## Usage

```bash
./run.sh create    # キャラ＆シナリオ作成
./run.sh play      # プレイ開始
./run.sh continue  # 前回の続き
```

## Requirements

- Claude Code CLI
